package eu.coatrack.admin.e2e.tests;

public class PageReachabilityTests extends AbstractTestSetup{

    //TODO I would like to check, if the menu on the left side works correctly and grants access to every page.
    //Because normally a page is reached directly via URL (driver.get()) and not by clicking.

}
